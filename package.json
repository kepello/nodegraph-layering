{
  "name": "@kepello/nodegraph-layering",
  "version": "0.5.4",
  "description": "Layering analysis for recovered clusters (Lakos levelization + violation detection + DSM rendering). Fourth layer of the Layered Code Abstraction arc.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist/",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test --import tsx 'src/**/*.test.ts'",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "@kepello/nodegraph-clusters": "^0.17.0",
    "@kepello/nodegraph-core": "^5.7.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@kepello/nodegraph-core": "file:../nodegraph-core",
    "@kepello/nodegraph-clusters": "file:../nodegraph-clusters"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "license": "MIT",
  "author": "Carl Lance",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kepello/nodegraph-layering.git"
  }
}
